spring:
  application:
    name: lestock
  datasource:
    url: jdbc:postgresql://localhost:5432/lestock_db
    username: lestock
    password: pass321
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true